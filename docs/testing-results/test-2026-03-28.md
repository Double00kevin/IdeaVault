# AIdeaPulse Engineering Verification — 2026-03-28

**Run by:** Claude Code on KITT
**Commit:** dcc8bc6 docs: sync KITT docs with Cowork — Sprint 3/4 changelog, open items, roadmap
**Timestamp:** 2026-03-28T12:50:20Z

---

## Summary

| # | Check | Status | Notes |
|---|-------|--------|-------|
| 1 | D1 Schema Integrity | PASS | All 8 expected tables present, all 6 migrations applied, columns match |
| 2 | Workers API Routes | PASS | 10/10 routes responding with expected status codes |
| 3 | Content Gating | FAIL | Deployed API returns ALL fields for unauthenticated users — gating not deployed |
| 4 | CORS Headers | PASS | Good origin allowed, evil origin rejected correctly |
| 5 | Auth Middleware Wiring | PASS | All routes correctly use requireAuth/optionalAuth per design |
| 6 | Stripe Webhook Verification | PASS | All 3 events handled, signature verified, secrets from env |
| 7 | Pipeline Connectivity | KNOWN ISSUE | Webhook URL points to workers.dev (not prod), 3/28 run timed out, module import fails |
| 8 | Test Suites | WARN | Pipeline 15/15 pass; Workers 0 tests (vitest can't find wrangler.toml, uses .jsonc) |
| 9 | Frontend Build | PASS | Builds clean, but missing privacy.astro and terms.astro pages |
| 10 | DNS/Routing | WARN | aideapulse.com 200, api 200, www does not resolve |
| 11 | Data Freshness | WARNING | Only 2026-03-27 has data (60 ideas) — 3/28 run killed by timeout |

**Overall:** 5/11 PASS, 1/11 FAIL, 3/11 WARNING, 2/11 KNOWN ISSUE

---

## Detailed Results

### 1. D1 Schema Integrity

**Tables found (10 total, 8 expected + 2 system):**
- `_cf_KV` (system)
- `d1_migrations` ✓
- `daily_free_claims` ✓
- `email_preferences` ✓
- `ideas` ✓
- `rate_limits` ✓
- `saved_ideas` ✓
- `sqlite_sequence` (system)
- `subscriptions` ✓
- `user_profiles` ✓

**All 6 migrations applied:**
- 0001_create_ideas.sql ✓
- 0002_expand_source_types.sql ✓
- 0003_user_saved_ideas.sql ✓
- 0004_subscriptions_rate_limits.sql ✓
- 0005_user_profiles.sql ✓
- 0006_daily_free_claims.sql ✓

**Column listings per table:**

| Table | Columns |
|-------|---------|
| ideas | id (TEXT PK), title, title_normalized, one_liner, problem_statement, target_audience, market_size_json, competitors_json, competitor_count (INT), build_complexity, build_timeline, monetization_angle, confidence_score, source_links_json, source_type, created_at |
| saved_ideas | id (INT PK), user_id, idea_id, rating (INT), saved_at |
| subscriptions | user_id (TEXT PK), stripe_customer_id, stripe_subscription_id, plan (default 'free'), status (default 'active'), created_at |
| rate_limits | key (TEXT PK), request_count (INT default 0) |
| user_profiles | user_id (TEXT PK), skills_json (default '[]'), budget_range (default 'low'), niches_json (default '[]'), experience_level (default 'beginner'), updated_at |
| daily_free_claims | user_id (TEXT PK), idea_id, claimed_date (TEXT PK), created_at |
| email_preferences | user_id (TEXT PK), email (default ''), frequency (default 'off'), subscribed_at |
| d1_migrations | id (INT PK), name, applied_at (TIMESTAMP) |

### 2. Workers API Routes

| Route | Method | Expected | Actual | Response Snippet |
|-------|--------|----------|--------|------------------|
| /api/health | GET | 200 | 200 | `{"status":"ok","ideas_count":60,"timestamp":"2026-03-28T12:50:23.420Z"}` |
| /api/ideas | GET | 200 | 200 | JSON array with 3 ideas (default limit), has_more=true, next_cursor present |
| /api/ideas?limit=1 | GET | 200 | 200 | JSON with 1 idea returned |
| /api/ideas/:id | GET | 200 | 200 | Full idea JSON for id c3505107... |
| /api/subscription | GET | 401 | 401 | `{"error":"Missing or invalid Authorization header"}` |
| /api/profile | GET | 401 | 404 | `{"error":"Not found"}` — **NOTE: returns 404 not 401** |
| /api/og/:id | GET | 200 | 200 | Content-Type: image/svg+xml, Cache-Control: public, max-age=86400 |
| /api/ingest (no HMAC) | POST | 401/403 | 401 | `{"error":"Missing auth headers"}` |
| /api/saved | GET | 401 | 401 | `{"error":"Missing or invalid Authorization header"}` |
| /api/stripe/webhook | POST | — | — | (not tested live — covered in code review check 6) |
| /api/stripe/checkout | POST | — | — | (requires auth — covered in code review check 5) |
| /api/digest/preferences | GET | — | — | (requires auth — covered in code review check 5) |
| /api/digest/send | POST | — | — | (webhook-protected — covered in code review check 5) |

**Note:** GET /api/profile returns 404 instead of 401 for unauthenticated requests. The route uses requireAuth middleware which should return 401 before reaching the handler. This suggests the 404 may come from route ordering or a handler-level check.

### 3. Content Gating

**FAIL — Content gating is NOT enforced on the deployed API.**

Per ADR-004 and the code in `workers/src/routes/ideas.ts`, unauthenticated (anon) users should receive only stripped fields via `stripIdeaFields()`:
- **Expected stripped fields:** id, title, source_type, confidence_score, created_at, build_complexity
- **Expected absent fields:** one_liner, problem_statement, target_audience, market_size, competitors, build_timeline, monetization_angle, source_links

**Actual API response for GET /api/ideas (unauthenticated):**

| Field | Expected | Actual |
|-------|----------|--------|
| id | Present | Present |
| title | Present | Present |
| source_type | Present | Present |
| confidence_score | Present | Present |
| created_at | Present | Present |
| build_complexity | Present | Present |
| one_liner | ABSENT | **PRESENT** |
| problem_statement | ABSENT | **PRESENT** |
| target_audience | ABSENT | **PRESENT** |
| market_size | ABSENT | **PRESENT** |
| competitors | ABSENT | **PRESENT** |
| competitor_count | — | Present |
| build_timeline | ABSENT | **PRESENT** |
| monetization_angle | ABSENT | **PRESENT** |
| source_links | ABSENT | **PRESENT** |
| tier | Present | **NOT PRESENT** |

**Conclusion:** The local code in `workers/src/routes/ideas.ts` correctly implements tier-based stripping (anon → stripIdeaFields, free → stripIdeaFields + daily claim, pro → full). However, the **deployed Worker does not match the local code**. The content gating changes have not been deployed to production.

**GET /api/ideas/:id (unauthenticated):** Also returns all fields with no gating or `gated: true` flag.

### 4. CORS Headers

**Preflight with Origin: https://aideapulse.com (legitimate):**
```
HTTP/2 204
access-control-allow-origin: https://aideapulse.com
access-control-allow-headers: Content-Type,Authorization
access-control-allow-methods: GET,POST,DELETE
vary: Origin, Access-Control-Request-Headers
```
✓ All required headers present.

**Preflight with Origin: https://evil.com (malicious):**
```
HTTP/2 204
access-control-allow-headers: Content-Type,Authorization
access-control-allow-methods: GET,POST,DELETE
vary: Origin, Access-Control-Request-Headers
```
✓ `access-control-allow-origin` header is **NOT present** — evil origin correctly rejected.

### 5. Auth Middleware Wiring (Code Review)

**Middleware definitions:** `workers/src/middleware/auth.ts`
- `requireAuth()` — lines 122-140: validates Bearer token, returns 401 if missing/invalid, sets userId on context
- `optionalAuth()` — lines 145-159: validates token if present, continues without auth silently

**Route → Middleware mapping:**

| Route | Middleware | Correct? |
|-------|-----------|----------|
| GET /api/health | None | ✓ Yes — fully public |
| GET /api/og/:id | None | ✓ Yes — fully public |
| POST /api/ingest | None (HMAC webhook auth) | ✓ Yes — uses X-Webhook-Signature |
| GET /api/ideas | optionalAuth() | ✓ Yes — tier-based gating |
| GET /api/ideas/:id | optionalAuth() | ✓ Yes — tier-based gating |
| GET /api/saved | requireAuth() | ✓ Yes |
| POST /api/saved/:ideaId | requireAuth() | ✓ Yes |
| DELETE /api/saved/:ideaId | requireAuth() | ✓ Yes |
| GET /api/profile | requireAuth() | ✓ Yes |
| POST /api/profile | requireAuth() | ✓ Yes |
| GET /api/subscription | requireAuth() | ✓ Yes |
| GET /api/digest/preferences | requireAuth() | ✓ Yes |
| POST /api/digest/preferences | requireAuth() | ✓ Yes |
| POST /api/digest/send | None (webhook secret auth) | ✓ Yes |
| POST /api/stripe/checkout | requireAuth() | ✓ Yes |
| POST /api/stripe/webhook | None (Stripe signature auth) | ✓ Yes |

**Clerk JWKS URL:** Dynamically derived from JWT token's `iss` claim → `${payload.iss}/.well-known/jwks.json`. Frontend Clerk publishable key is `pk_live_Y2xlcmsuYWlkZWFwdWxzZS5jb20k` which decodes to `clerk.aideapulse.com` — **confirmed production instance**.

### 6. Stripe Webhook Verification (Code Review)

**File:** `workers/src/routes/stripe.ts`

**Signature verification:**
- ✓ Checks for `stripe-signature` header (line 106)
- ✓ Returns 400 if missing
- ✓ Uses `STRIPE_WEBHOOK_SECRET` from `c.env` (not hardcoded) (line 101)
- ✓ Implements `verifyStripeSignature()` (lines 55-97):
  - Parses `t=` timestamp and `v1=` signature from header
  - Validates timestamp within 5-minute window
  - HMAC-SHA256 with `crypto.subtle`
  - **Timing-safe comparison** (byte-by-byte, lines 90-96)

**Event handlers:**

| Event Type | Handled? | D1 Operation | Table |
|-----------|----------|-------------|-------|
| checkout.session.completed | ✓ Lines 120-139 | INSERT OR REPLACE (upsert) | subscriptions |
| customer.subscription.updated | ✓ Lines 142-151 | UPDATE status | subscriptions |
| customer.subscription.deleted | ✓ Lines 154-161 | UPDATE status→canceled | subscriptions |

All handlers correctly write to the `subscriptions` table. The checkout handler extracts `clerk_user_id` from session metadata for the user_id foreign key.

### 7. Pipeline Connectivity

| Check | Result |
|-------|--------|
| ANTHROPIC_API_KEY in .env | ✓ PRESENT |
| INGEST_WEBHOOK_URL in .env | ✓ PRESENT |
| INGEST_WEBHOOK_SECRET in .env | ✓ PRESENT |
| WEBHOOK_URL value | `https://aideapulse-api.double00kevin.workers.dev/api/ingest` |
| Module import | **FAIL** — `ModuleNotFoundError: No module named 'pipeline'` |
| httpx installed | ✓ httpx 0.28.1 |
| systemd timer | ✓ active (waiting), next trigger: 2026-03-29 06:00:00 CDT |
| systemd service (last run) | **FAILED** — timeout at 2026-03-28 06:10:04 CDT |

**Issues found:**

1. **INGEST_WEBHOOK_URL points to old workers.dev domain**, not the production custom domain `https://api.aideapulse.com/api/ingest`. The pipeline is POSTing ingested data to the development/default Workers URL. This works (both domains route to the same Worker) but should be updated for consistency and in case the workers.dev route is ever restricted.

2. **Pipeline variable naming:** The .env uses `INGEST_WEBHOOK_URL` and `INGEST_WEBHOOK_SECRET` (matching `config.py`), not `WEBHOOK_URL`/`WEBHOOK_SECRET` as the prompt expected. The config is correct — the prompt's expected names were wrong.

3. **Module import fails:** Running `python -m pipeline.main` works (per systemd ExecStart), but `from pipeline.main import *` fails when CWD is `pipeline/`. This is a PYTHONPATH issue — `pyproject.toml` sets `pythonpath = ["..", "."]` for pytest, but direct invocation needs `-m` flag. **Not a real bug** — systemd runs it correctly with `-m pipeline.main`.

4. **Known issue confirmed:** 2026-03-28 run timed out (SIGTERM after TimeoutStartSec=600). Service was actively making Claude API calls when killed. Need to increase `TimeoutStartSec` to ~1200s or more.

### 8. Test Suites

**Pipeline tests:**
```
15 passed in 0.28s
```
- test_analysis.py: 6/6 passed (JSON parsing, malformed input, confidence clamping, markdown stripping, title truncation)
- test_prefilter.py: 6/6 passed (Reddit/PH/Trends filtering)
- test_push.py: 3/3 passed (HMAC signature generation)

**Workers tests:**
```
0 tests — 1 error
```
- Error: `ParseError: Could not read file: /home/doubl/AIdeaPulse/workers/wrangler.toml`
- The project uses `wrangler.jsonc` (not `.toml`). The vitest-pool-workers plugin is looking for `wrangler.toml`.
- **Root cause:** vitest config likely needs `configPath` set to `wrangler.jsonc`.

### 9. Frontend Build

**Build:** ✓ Completed in 2.27s — no errors, no warnings (aside from informational Cloudflare adapter messages).

**Pages in `src/pages/`:**

| Page | Exists? |
|------|---------|
| index.astro | ✓ |
| dashboard.astro | ✓ |
| about.astro | ✓ |
| pro.astro | ✓ |
| ideas/ (directory) | ✓ (ideas/[id].astro) |
| 404.astro | ✓ |
| privacy.astro | **MISSING** |
| terms.astro | **MISSING** |

**Known pre-launch gap:** privacy.astro and terms.astro pages do not exist.

**API Base URL:**
- Most components: `import.meta.env.PUBLIC_API_URL ?? "/api"` — uses relative path, correct for CF Pages proxying to Workers
- `src/pages/ideas/[id].astro`: `import.meta.env.PUBLIC_API_URL ?? "http://localhost:8787/api"` — **localhost fallback is wrong for SSR on CF Pages** (should be `/api` or the full production URL). However, since `PUBLIC_API_URL` is set in `.env` to `https://api.aideapulse.com/api`, this fallback is only used if the env var is missing.
- `.env` value: `PUBLIC_API_URL=https://api.aideapulse.com/api` ✓

**Clerk publishable key:**
- `.env`: `PUBLIC_CLERK_PUBLISHABLE_KEY=pk_live_Y2xlcmsuYWlkZWFwdWxzZS5jb20k`
- ✓ Starts with `pk_live_` — production key confirmed.

### 10. DNS/Routing

| URL | Status | Headers/Notes |
|-----|--------|---------------|
| https://aideapulse.com | 200 | content-type: text/html, server: cloudflare, cf-cache-status: DYNAMIC |
| https://api.aideapulse.com/api/health | 200 | content-type: application/json, server: cloudflare |
| https://www.aideapulse.com | **NO RESPONSE** | DNS does not resolve — no A/CNAME record for www subdomain |

**Issue:** `www.aideapulse.com` does not resolve. Need to add a CNAME or redirect rule in Cloudflare DNS.

### 11. Data Freshness

```sql
SELECT date(created_at) as day, COUNT(*) as cnt FROM ideas GROUP BY day ORDER BY day DESC LIMIT 7
```

| Day | Count |
|-----|-------|
| 2026-03-27 | 60 |

**Only one day of data.** The 2026-03-28 pipeline run was killed by the systemd timeout (TimeoutStartSec=600s), so no new ideas were ingested today. The pipeline was actively processing Claude API responses when terminated.

---

## Action Items

1. **[CRITICAL] Deploy content gating to production** — The local code in `workers/src/routes/ideas.ts` correctly implements tier-based field stripping, but the deployed Worker still returns all fields to unauthenticated users. Run `npx wrangler deploy` from `workers/`.

2. **[HIGH] Fix pipeline webhook URL** — Update `INGEST_WEBHOOK_URL` in `pipeline/.env` from `https://aideapulse-api.double00kevin.workers.dev/api/ingest` to `https://api.aideapulse.com/api/ingest`.

3. **[HIGH] Increase pipeline systemd timeout** — Change `TimeoutStartSec` from 600 to 1200+ in `/etc/systemd/system/aideapulse-pipeline.service` to prevent timeout kills during full 8-source ingestion.

4. **[HIGH] Add www.aideapulse.com DNS** — Add a CNAME record for `www` → `aideapulse.com` (or a redirect rule) in Cloudflare DNS.

5. **[MEDIUM] Fix workers vitest config** — Update vitest config to point to `wrangler.jsonc` instead of `wrangler.toml` so the test suite can run.

6. **[MEDIUM] Create privacy.astro and terms.astro** — Required for launch. These are known missing pages.

7. **[LOW] Fix localhost fallback in ideas/[id].astro** — Change `"http://localhost:8787/api"` fallback to `"/api"` to match other components. Not a production issue (env var is set) but a safety concern.

8. **[LOW] Investigate /api/profile returning 404** — GET /api/profile without auth returns 404 instead of the expected 401. The requireAuth middleware should intercept before the handler. May be a route ordering issue.

## Known Issues (Pre-Existing)

1. **2026-03-28 pipeline timeout** — TimeoutStartSec=600 is too short for a full run with 8 sources + Claude API analysis. Pipeline was actively processing when killed.
2. **Missing privacy/terms pages** — Known pre-launch gap documented in Sprint 4 planning.
3. **Pipeline module import** — `from pipeline.main import *` fails when CWD is `pipeline/`, but the systemd service correctly uses `python -m pipeline.main`. Not a real bug.
